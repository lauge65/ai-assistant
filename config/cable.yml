development:
  adapter: async

test:
  adapter: test

production:
  adapter: <%= ENV.fetch("REDIS_URL", nil) ? "redis" : "async" %>
  url: <%= ENV.fetch("REDIS_URL", nil) %>
  channel_prefix: ai_assistant_production
