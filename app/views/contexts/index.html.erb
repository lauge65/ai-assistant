<div class="container my-5">

  <%= @contexts.each do |context| %>

  <div class="row">
    <%= context.title %>
    <%= context.subject %>
    <%= context.level %>
    <%= context.date %>

    <%= link_to context_path(context), class: "action-bnt" do %>
    show
    <% end %>
  </div>





</div>
